import 'package:flutter/material.dart';

// Using the same color scheme as the customer app for consistency
const kPrimaryColor = Color(0xFFE07E3D);
const kPrimaryLightColor = Color(0xFFBDE5E5);
const kTextColor = Color(0xFF3C4046);
const double defaultPadding = 16.0;

// Additional colors for driver-specific UI elements
const kActiveColor = Color(0xFF4CAF50);  // Green for active status
const kInactiveColor = Color(0xFF9E9E9E); // Grey for inactive status
const kEmergencyColor = Color(0xFFF44336); // Red for emergency actions